---

mindmap-plugin: basic

---

# 工作笔记

## 华为静态VXLAN基础配置
- VTEP设备
	- BD域配置
		- bridge-domain <>
		- vxlan vni <>
		- bd和vni无需与vid保持一致，仅做区分之用
	- NVE接口配置
		- int nve <>
		- source <本端VTEP源IP>
		- vni  <> head-end peer-list <对端VTEP的IP>
	- 配置二层子接口
		- int <> mode L2
		- encapsulation dot1q vid <>
		- bridge-domain <>
		- statistics enable
			- 开启流统功能
- 接入设备
	- 创建业务VLAN
		- vlan <>
		- description <>
	- 上行端口配置
		- p l h
		- p h t v <>
	- 客户接入端口配置
		- 根据情况放行vlan

## OSPF故障排查
- OSPF建立故障排查
	- 卡在down/init状态
		- 接口未使能ospf
			- netowork
		- 接口配置了静默接口配置
			- silent-interface
		- ACL或防火墙过滤了ospf报文
			- 协议号89
			组播地址224.0.0.5/224.0.0.6
		- NBMA网络未手动指定邻居
			- peer
		- 互联端口IP地址掩码不一致
		- 是否采用从地址建立邻居
			- 从地址无法建立邻居关系
	- 卡在2-way/attempt状态
		- MA网络将进行DR/BDR选举（40S），非DR/BDR间不能进入下一状态
		- 所有路由优先级均为0，则不进行DR/BDR选举
			- ospf dr-priority 0
	- 卡在exstart/exchange状态
		- 接口MTU不一致
		- 两端单播IP不可达
		- 互联链路质量差
		- RID冲突
	- 卡在loading状态
		- MTU问题
			- 导致大LSU报文丢弃
			- 即使 DBD 交换完成，携带大量 LSA 的 LSU 也可能因 MTU 不匹配被丢弃
		- 请求的LSA校验错误或不可用
			- 例如 Router ID 冲突导致 LSA 混乱。可用 display ospf error 观察错误增长趋势，结合 display ospf brief 检查全网 Router ID 是否唯一
	- 其他原因
		- 区域ID不一致
		- 区域类型不一致
		- hello/dead时间不一致
		- 认证失败
		- 网络类型不一致
		- 互联IP不在同一网段
		- NBMA/P2MP未指定邻居
		- 接口down，物理层或协议没有UP
		- CPU/内存过高导致设备没有多余资源处理OSPF报文
		- BFD会话DOWN
- OSPF路由无法通告故障排查
	- 无法通告从地址路由
		- 从地址与主地址区域不同
	- ABR无法通告路由
		- 区域不允许接收汇总路由
			- 完全存根/完全NSSA区域的ABR不会向该区域通告其他区域路由
		- ABR与骨干区域隔离
			- 与ABR相连的区域必须有一个骨干区域，如果没有骨干则无法通告区域间路由
		- OSPF骨干区域分割
			- ABR不能通过非骨干区域学习区域间路由
	- 无法通告外部路由
		- 区域不允许接收外部路由
			- 存根/完全存根/NSSA/完全NSSA区域的话外部路由是无法向这些区域通告的
		- NSSA区域多ABR
			- 如果NSSA区域存在多个ABR，具有较大RouterID的ABR才会进行类型7的LSA转换成类型5的LSA操作，其它的ABR不能进行这个转换，如果NSSA区域存在配置错误的ABR而且其RouterID较大，那么该NSSA区域的外部路由可能无法通告到其它区域。
- OSPF路由无法加入路由表故障排查
	- 路由表中没有OSPF路由
		- 网络类型不匹配
		- 点到点网络单边无编号
			- 在OSPF中，表达链路使用借用地址的LSA中所携带的链路数据值与一般链路不同。点到点网络中，如果一端配置有IP地址，另外一边配置为借用IP地址，则二者的LSA链路数据值不匹配，这就导致了OSPF数据库中的不一致，因此不会在OSPF路由表中添加路由。
	- 外部路由无法加入路由表
		- FA（Forwarding Address）地址有效性检查失败
			- OSPF外部路由中会携带转发地址信息，如果该转发地址非零，那么OSPF必须能够通过区域内或区域间路由到达该转发地址，否则该外部路由不会加入OSPF路由表。

## 丢包故障排查
- 检查物理层面
	- 查看光模块与光衰是否正常，光模块兼容性、温度是否过高
	- 查看接口是否存在CRC、FCS增长
	- 检查网线是否超过100M或老化/折损/压伤/氧化
	- 替换法或其他方式确定是否是设备本体故障
- 检查设备CPU/内存利用率
- 网络环路
	- 检查是否存在MAC地址漂移
	- CPU异常
- 排查ARP/IP
	- 检查是否存在IP地址冲突
- 检查带宽拥塞
	- 查看接口利用率